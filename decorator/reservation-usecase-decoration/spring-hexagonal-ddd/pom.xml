<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.3</version>
		<relativePath/>
	</parent>

	<groupId>com.germarna.patterns.decorator.hexagonalddd</groupId>
	<artifactId>reservation-service-decorator-example</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>Reservation Service Decorator Example</name>
	<description>
		A Spring Boot example demonstrating the Decorator Pattern applied to a use case
		in a DDD + Hexagonal Architecture context.
	</description>

	<properties>
		<!-- Java & Spring Boot -->
		<java.version>17</java.version>
		<!-- Dependency versions -->
		<lombok.version>1.18.38</lombok.version>
		<pitest.junit5.version>1.2.1</pitest.junit5.version>
		<jacoco.version>0.8.12</jacoco.version>
		<maven.surefire.version>3.1.2</maven.surefire.version>
		<maven.failsafe.version>3.1.2</maven.failsafe.version>
		<pitest.maven.version>1.20.1</pitest.maven.version>
	</properties>

	<dependencies>
		<!-- Spring Boot -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<!-- Lombok -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>${lombok.version}</version>
			<scope>provided</scope>
		</dependency>

		<!-- Testing -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-core</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- PIT JUnit5 plugin -->
		<dependency>
			<groupId>org.pitest</groupId>
			<artifactId>pitest-junit5-plugin</artifactId>
			<version>${pitest.junit5.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Compiler plugin -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>

			<!-- Spring Boot plugin -->
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>

			<!-- Surefire: run unit tests -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${maven.surefire.version}</version>
				<configuration>
					<includes>
						<include>**/*Test.java</include>
					</includes>
				</configuration>
			</plugin>

			<!-- Failsafe: run integration tests -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<version>${maven.failsafe.version}</version>
				<configuration>
					<includes>
						<include>**/*IT.java</include>
					</includes>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<!-- JaCoCo: separate coverage for UT and IT -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<executions>
					<!-- Agent for Unit Tests (Surefire) -->
					<execution>
						<id>prepare-agent-ut</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
						<configuration>
							<!-- UT coverage file -->
							<destFile>${project.build.directory}/jacoco-ut.exec</destFile>
						</configuration>
					</execution>

					<!-- Unit Test report -->
					<execution>
						<id>report-ut</id>
						<phase>test</phase>
						<goals>
							<goal>report</goal>
						</goals>
						<configuration>
							<dataFile>${project.build.directory}/jacoco-ut.exec</dataFile>
							<outputDirectory>${project.reporting.outputDirectory}/jacoco-ut</outputDirectory>
							<excludes>
								<!-- Exclude these from UT report only (they remain in IT) -->
								<exclude>com/germarna/patterns/decorator/hexagonalddd/adapter/in/usecase/decorator/AuthorizationCreateReservationUseCaseDecorator*</exclude>
								<exclude>com/germarna/patterns/decorator/hexagonalddd/adapter/in/usecase/decorator/LoggingCreateReservationUseCaseDecorator*</exclude>
								<exclude>com/germarna/patterns/decorator/hexagonalddd/adapter/in/usecase/decorator/MetricsCreateReservationUseCaseDecorator*</exclude>
								<exclude>com/germarna/patterns/decorator/hexagonalddd/adapter/in/usecase/decorator/TransactionalCreateReservationUseCaseDecorator*</exclude>
								<exclude>com/germarna/patterns/decorator/hexagonalddd/adapter/out/persistence/**</exclude>
								<exclude>com/germarna/patterns/decorator/hexagonalddd/ApplicationBoot*</exclude>
							</excludes>
						</configuration>
					</execution>

					<!-- Agent for Integration Tests (Failsafe) -->
					<execution>
						<id>prepare-agent-it</id>
						<phase>pre-integration-test</phase>
						<goals>
							<goal>prepare-agent-integration</goal>
						</goals>
						<configuration>
							<!-- IT coverage file -->
							<destFile>${project.build.directory}/jacoco-it.exec</destFile>
						</configuration>
					</execution>

					<!-- Integration Test report -->
					<execution>
						<id>report-it</id>
						<phase>verify</phase>
						<goals>
							<goal>report</goal>
						</goals>
						<configuration>
							<dataFile>${project.build.directory}/jacoco-it.exec</dataFile>
							<outputDirectory>${project.reporting.outputDirectory}/jacoco-it</outputDirectory>
							<excludes>
								<exclude>com/germarna/patterns/decorator/hexagonalddd/ApplicationBoot*</exclude>
							</excludes>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- PIT Mutation Testing -->
			<plugin>
				<groupId>org.pitest</groupId>
				<artifactId>pitest-maven</artifactId>
				<version>${pitest.maven.version}</version>
				<configuration>
					<targetClasses>
						<param>com.germarna.patterns.decorator.hexagonalddd.*</param>
					</targetClasses>
					<targetTests>
						<param>com.germarna.patterns.decorator.hexagonalddd.*</param>
					</targetTests>
					<excludedClasses>
						<param>com.germarna.patterns.decorator.hexagonalddd.ApplicationBoot*</param>
						<param>com.germarna.patterns.decorator.hexagonalddd.adapter.in.usecase.decorator.AuthorizationCreateReservationUseCaseDecorator*</param>
						<param>com.germarna.patterns.decorator.hexagonalddd.adapter.in.usecase.decorator.LoggingCreateReservationUseCaseDecorator*</param>
						<param>com.germarna.patterns.decorator.hexagonalddd.adapter.in.usecase.decorator.MetricsCreateReservationUseCaseDecorator*</param>
						<param>com.germarna.patterns.decorator.hexagonalddd.adapter.in.usecase.decorator.TransactionalCreateReservationUseCaseDecorator*</param>
					</excludedClasses>
					<mutators>
						<mutator>STRONGER</mutator>
						<mutator>-VOID_METHOD_CALLS</mutator>
					</mutators>
					<threads>4</threads>
					<outputFormats>
						<param>HTML</param>
						<param>XML</param>
					</outputFormats>
					<failWhenNoMutations>true</failWhenNoMutations>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
